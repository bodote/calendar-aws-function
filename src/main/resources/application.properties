gg.jte.development-mode=true
spring.application.name=calendar-lambda

# Spring Cloud Function Configuration
# Isolate the function handler to a specific path prefix (/api) to prevent
# it from conflicting with Spring MVC controllers and static resources.
spring.cloud.function.web.path-prefix=/api

# Static resource configuration
spring.web.resources.static-locations=classpath:/static/
